{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && npm run build"
  },
  "deploy": {
    "startCommand": "npm run db:push && NODE_ENV=production node dist/index.cjs",
    "restartPolicyType": "ON_FAILURE"
  }
}
